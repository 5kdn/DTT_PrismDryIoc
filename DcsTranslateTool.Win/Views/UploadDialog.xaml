<controls:MetroWindow
    x:Class="DcsTranslateTool.Win.Views.UploadDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="http://metro.mahapps.com/winfx/xaml/controls"
    xmlns:properties="clr-namespace:DcsTranslateTool.Win.Properties"
    Style="{StaticResource CustomMetroWindow}"
    mc:Ignorable="d"
    Title="Upload">
    <Grid Margin="{StaticResource MediumLeftRightMargin}">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <StackPanel>
            <TextBlock Text="選択ファイル" FontWeight="Bold" Margin="0,0,0,8" />
            <ListBox ItemsSource="{Binding FilePaths}" Height="100" />
            <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                <ComboBox Width="100" ItemsSource="{Binding CategoryOptions}" SelectedItem="{Binding SelectedCategory}" />
                <ComboBox Width="150" Margin="8,0,0,0" ItemsSource="{Binding NameOptions}" SelectedItem="{Binding SelectedName}" />
            </StackPanel>
            <TextBlock Text="変更内容" FontWeight="Bold" Margin="0,8,0,4" />
            <ItemsControl ItemsSource="{Binding ChangeItems}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <CheckBox Content="{Binding Text}" IsChecked="{Binding IsChecked}" />
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
            <TextBlock Text="コメント" FontWeight="Bold" Margin="0,8,0,4" />
            <TextBox Text="{Binding Comment}" AcceptsReturn="True" Height="80" />
            <StackPanel Margin="0,8,0,0">
                <CheckBox Content="アップロードするファイルやコメントに個人情報は記載していないことを確認しましたか？" IsChecked="{Binding ConfirmPersonal}"/>
                <CheckBox Content="翻訳していないファイルをアップロードしていないことを確認しましたか？" IsChecked="{Binding ConfirmTranslate}"/>
            </StackPanel>
        </StackPanel>
        <Button Grid.Row="1" Content="Commit" HorizontalAlignment="Right" Margin="0,8,0,0" Width="80" IsEnabled="{Binding CanCommit}" Command="{Binding CommitCommand}" />
    </Grid>
</controls:MetroWindow>
