<UserControl
    x:Class="DcsTranslateTool.Win.Views.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:properties="clr-namespace:DcsTranslateTool.Win.Properties"
    Style="{StaticResource BaseUserControlStyle}"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="48" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <!-- The Mahapps Metro title bar -->
        <Grid Margin="{StaticResource MediumLeftRightMargin}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock
                Style="{StaticResource PageTitleStyle}"
                Margin="{StaticResource MediumLeftMargin}"
                Text="{x:Static properties:Resources.MainPageTitle}" />
            <Button
              Grid.Column="1"
              Margin="{StaticResource SmallLeftMargin}"
              Content="{x:Static properties:Resources.SettingsPageTitle}"
              Command="{Binding OpenSettingsCommand}" />
        </Grid>
        <!-- The content area where the main content will be placed -->
        <Grid
            Grid.Row="1"
            Margin="{StaticResource MediumLeftRightMargin}"
            Background="{DynamicResource MahApps.Brushes.Gray10}">
            <!--
            The Mahapps Gray10 color represents where you should place your content.
            Place your content here.
            -->
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Button>
                    <StackPanel>
                        <TextBlock Text="{x:Static properties:Resources.DownloadPageTitle}"/>
                    </StackPanel>
                </Button>

                <Button Grid.Column="1">
                    <StackPanel>
                        <TextBlock Text="{x:Static properties:Resources.UploadPageTitle}"/>
                    </StackPanel>
                </Button>

                <Button Grid.Column="2">
                    <StackPanel>
                        <TextBlock Text="{x:Static properties:Resources.SettingsPageTitle}"/>
                    </StackPanel>
                </Button>            
            </Grid>
          
          
        </Grid>
    </Grid>
</UserControl>
